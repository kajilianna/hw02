论文导读
论文信息
- 论文标题：Attention Is All You Need
- 出处：NeurIPS 2017
- 使用大模型：DeepSeek
1. 研究背景与动机
在Transformer模型提出之前，深度学习在自然语言处理领域的主流架构主要以循环神经网络（RNN、LSTM、GRU） 及其变体为主。这类模型依靠时序递归的方式处理序列数据，存在两个明显缺陷：
1. 并行计算能力差：必须按顺序逐个处理token，无法充分利用GPU算力。
2. 长距离依赖建模弱：序列过长时，梯度容易消失或爆炸，难以捕捉远距离词语之间的关系。
尽管此前已有部分工作尝试结合卷积或注意力机制提升效率，但始终没有一种架构能同时做到高效并行与强大长距离建模。
因此，本文提出一种完全基于自注意力机制的新架构——Transformer，旨在摆脱对循环和卷积的依赖，用更简单、更高效的方式完成序列建模。
2. 核心方法
Transformer的核心创新是多头自注意力（Multi-Head Attention） 和位置编码（Positional Encoding），整体采用编码器-解码器（Encoder-Decoder） 结构。
2.1 编码器（Encoder）
由N个相同层堆叠而成，每层包含：
- 多头自注意力：让每个词同时关注句子中所有词的信息。
- 前馈神经网络（FFN）。
- 残差连接 + 层归一化。
2.2 解码器（Decoder）
同样由N个层堆叠，在编码器基础上增加：
- 掩码自注意力：防止训练时看到未来位置的信息。
- 编码器-解码器注意力：用于在生成时关注输入序列。
2.3 多头自注意力
将Q、K、V映射到多个低维空间，并行计算注意力再拼接，能同时捕捉不同类型、不同位置的依赖关系，比单头注意力表达能力更强。
2.4 位置编码
因为Transformer没有时序结构，通过加入位置编码，为模型提供序列顺序信息，使其理解词语的位置关系。
3. 主要结果
论文在机器翻译任务上取得了当时SOTA（state-of-the-art） 效果：
- 在WMT 2014英德翻译任务上，BLEU值超过所有已有模型。
- 在英法翻译任务上，同样大幅超越之前基于RNN/CNN的模型。
- 训练速度大幅提升：比同等效果的RNN模型快非常多，更适合大规模预训练。
Transformer不仅在翻译任务成功，更成为后续BERT、GPT、T5等所有大语言模型的基础架构。
4. 总结与个人小结
《Attention Is All You Need》是近十年人工智能领域最具影响力的论文之一。
它用极简的思想证明：仅靠注意力机制，就足以完成高质量的序列建模，彻底改变了NLP的发展方向，并扩展到计算机视觉、语音、多模态等领域。
对今天学习大模型的人来说，Transformer是必须掌握的基础架构，理解它的注意力机制、编码器解码器结构，是读懂一切现代大模型的前提。
